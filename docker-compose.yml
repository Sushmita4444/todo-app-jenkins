services:
  flask:
    container_name: flask
    image: sushmita01/docker-flask
    ports:
      - 5000:5000
    depends_on:
      - db

  db:
    container_name: db
    image: sushmita01/docker
    environment:
      - MYSQL_ROOT_PASSWORD=password
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
